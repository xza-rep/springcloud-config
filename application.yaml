spring:
  profiles:
    active: dev

---
spring:
  profiles: dev
  application:
    name: springcloud-config-icoding-dev

---
spring:
  profiles: test
  application:
    name: springcloud-config-icoding-test